module.exports = {
  // the maximum line length
  maxWidth: 140,
  // the number of spaces per indentation-level
  tabWidth: 2,
  // indent lines with spaces
  useTabs: false,
  // semicolons at the ends of statements
  semi: true,
  // single quotes
  singleQuote: true,
  // quote properties in objects if needed
  quoteProps: 'as-needed',
  // trailing commas wherever possible
  trailingComma: 'all',
  // spaces between brackets in object literals
  bracketSpacing: true,
  // parentheses around arrow function parameters
  arrowParens: 'always',
  // wrap markdown text as-is since
  proseWrap: 'preserve',
};
